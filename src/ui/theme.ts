import pc from 'picocolors';

export const hadithBlue = (text: string): string => pc.cyan(text);
export const hadithGreen = (text: string): string => pc.green(text);
export const hadithYellow = (text: string): string => pc.yellow(text);
export const hadithDim = (text: string): string => pc.dim(text);
export const hadithBold = (text: string): string => pc.bold(text);

export const PRAYER_HANDS_EMOJI = '🤲';
export const STAR_EMOJI = '⭐';
export const BOOK_EMOJI = '📖';
