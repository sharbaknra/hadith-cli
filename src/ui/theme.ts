import pc from 'picocolors';

export const duaBlue = (text: string): string => pc.cyan(text);
export const duaGreen = (text: string): string => pc.green(text);
export const duaYellow = (text: string): string => pc.yellow(text);
export const duaDim = (text: string): string => pc.dim(text);
export const duaBold = (text: string): string => pc.bold(text);

export const PRAYER_HANDS_EMOJI = '🤲';
export const STAR_EMOJI = '⭐';
export const BOOK_EMOJI = '📖';
